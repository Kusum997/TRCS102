# Day 6 – Advanced JavaScript Array Methods (27 June W2)
---
## Introduction

On this day, I learned about **Advanced Array Methods** in JavaScript. These methods help in searching, filtering, copying, modifying, adding, and removing elements from an array efficiently. I also learned the difference between `slice()` and `splice()`.



# Advanced Array Methods

JavaScript provides several built-in methods to perform operations on arrays without writing lengthy code.

---

## 1. `filter()` Method

The `filter()` method creates a **new array** containing only the elements that satisfy a given condition.

### Syntax

```javascript
array.filter(function(element){
    return condition;
});
```

### Example

```javascript
let numbers = [10, 20, 30, 40, 50];

let result = numbers.filter(num => num > 25);

console.log(result);
```

**Output**

```text
[30, 40, 50]
```

---

## 2. `find()` Method

The `find()` method returns the **first element** that satisfies a given condition.

### Syntax

```javascript
array.find(function(element){
    return condition;
});
```

### Example

```javascript
let numbers = [10, 20, 30, 40, 50];

let result = numbers.find(num => num > 25);

console.log(result);
```

**Output**

```text
30
```

---

## 3. `slice()` Method

The `slice()` method returns a **new array** by copying a portion of the original array. It **does not modify** the original array.

### Syntax

```javascript
array.slice(startIndex, endIndex);
```

### Example

```javascript
let fruits = ["Apple", "Banana", "Mango", "Orange"];

let result = fruits.slice(1, 3);

console.log(result);
console.log(fruits);
```

**Output**

```text
["Banana", "Mango"]
["Apple", "Banana", "Mango", "Orange"]
```

---

## 4. `splice()` Method

The `splice()` method is used to **add**, **remove**, or **replace** elements in an array. It **modifies the original array**.

### Syntax

```javascript
array.splice(startIndex, deleteCount, item1, item2);
```

### Example

```javascript
let fruits = ["Apple", "Banana", "Mango"];

fruits.splice(1, 1);

console.log(fruits);
```

**Output**

```text
["Apple", "Mango"]
```

---

# Difference Between `slice()` and `splice()`

| `slice()` | `splice()` |
|-----------|------------|
| Returns a new array. | Modifies the original array. |
| Does not change the original array. | Changes the original array. |
| Used for copying a portion of an array. | Used to add, remove, or replace elements. |
| End index is not included. | Requires delete count and optional new elements. |

---

# Adding Elements Using `splice()`

```javascript
let fruits = ["Apple", "Mango"];

fruits.splice(1, 0, "Banana");

console.log(fruits);
```

**Output**

```text
["Apple", "Banana", "Mango"]
```

---

# Removing Elements Using `splice()`

```javascript
let fruits = ["Apple", "Banana", "Mango"];

fruits.splice(1, 1);

console.log(fruits);
```

**Output**

```text
["Apple", "Mango"]
```

---

# Replacing Elements Using `splice()`

```javascript
let fruits = ["Apple", "Banana", "Mango"];

fruits.splice(1, 1, "Orange");

console.log(fruits);
```

**Output**

```text
["Apple", "Orange", "Mango"]
```

---

# Modifying Array Elements

```javascript
let numbers = [10, 20, 30];

numbers[1] = 50;

console.log(numbers);
```

**Output**

```text
[10, 50, 30]
```

---

# Multiplying Array Elements Using `map()`

The `map()` method creates a new array by applying a function to every element.

```javascript
let numbers = [1, 2, 3, 4];

let result = numbers.map(num => num * 2);

console.log(result);
```

**Output**

```text
[2, 4, 6, 8]
```

---

# Complete Practice Program

```javascript
let numbers = [10, 20, 30, 40, 50];

// Filter
console.log(numbers.filter(num => num > 25));

// Find
console.log(numbers.find(num => num > 25));

// Slice
console.log(numbers.slice(1, 4));

// Splice
numbers.splice(2, 1, 100);
console.log(numbers);

// Map
let doubled = numbers.map(num => num * 2);
console.log(doubled);
```

**Output**

```text
[30, 40, 50]
30
[20, 30, 40]
[10, 20, 100, 40, 50]
[20, 40, 200, 80, 100]
```

---

# Key Learnings

- Learned advanced JavaScript array methods.
- Understood how `filter()` returns elements based on a condition.
- Learned that `find()` returns the first matching element.
- Practiced using `slice()` to copy a portion of an array.
- Used `splice()` to add, remove, and replace elements.
- Understood the difference between `slice()` and `splice()`.
- Modified array elements using indexes.
- Used `map()` to perform multiplication on array elements.

---

# Conclusion

In this session, I learned advanced JavaScript array methods such as `filter()`, `find()`, `slice()`, and `splice()`. I also practiced modifying arrays by adding, removing, replacing, and updating elements. These methods make array manipulation simple, efficient, and are widely used in modern JavaScript applications.
